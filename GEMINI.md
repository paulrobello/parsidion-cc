# Gemini Configuration

This project is a Claude Code customization toolkit (skills, hooks, agents, and an installer) that manages a markdown knowledge vault at `~/ClaudeVault/`. It is not a runnable application -- it is managed configuration and scripts.

For full project documentation, architecture details, and development workflow, see `@CLAUDE.md`.
